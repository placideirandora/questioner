language: node_js
node_js:
  - "stable"
services:
  - postgresql
before_script:
  - "psql -c 'create database questioner;' -U postgres"
script:
  - npm test
after_success:
  - npm run coveralls
  - codeclimate-test-reporter < ./coverage/lcov.info
 

               

